# تحلیل سودآوری ربات NobitexAgentWin — نمرهٔ نهایی: **۳۹ از ۱۰۰**

نسخهٔ بررسی‌شده: `6.9.0` (شاخهٔ `arena/01a0bcf4-nobitexagentwin`) · تاریخ: ۲۰۲۶/۰۹/۲۰
روش: بازبینی خط‌به‌خط منطق ورود/خروج و پیکربندی + بک‌تست ۱۰۰ روزه روی دادهٔ واقعی ۱۸ بازار + شبیه‌سازی مونت‌کارلوی هندسهٔ خروج + آزمون کادنس زندهٔ ۱۰ ثانیه‌ای.

> **دو مرحله، دو نمره:** تحلیل اولیه **۲۸ از ۱۰۰** بود (بند ۷). پس از پیاده‌سازی اصلاحاتِ بند ۸ در کد و تکرار مطالعه با دادهٔ بزرگ‌تر و مدل خروج دقیق‌تر، نمره **۳۹ از ۱۰۰** شد (بند ۱۱-۶). این ۱۱ امتیاز از کنترل ریسک، شفافیت و مهندسی می‌آید — **نه از کشف سود**: هیچ حالت واقع‌گرایانه‌ای سودده نشد و لبهٔ اندازه‌گیری‌شدهٔ سیگنال (≈ +۰٫۲۷٪ ناخالص در هر معامله) هنوز حدود یک‌سوم هزینهٔ رفت‌وبرگشت (≈ ۰٫۹٪) است.

> **جمع‌بندی یک‌خطی:** معماری نرم‌افزاری و کنترل ریسک این ربات از میانگین نمونه‌های مشابه بهتر است، اما **اقتصاد معاملات آن منفی است**: ساختار ورود/خروج به‌گونه‌ای است که در نبود یک «لبهٔ پیش‌بینی» اثبات‌شده، هر معامله به‌طور میانگین حدود هزینهٔ رفت‌وبرگشت (۰٫۷ تا ۱٫۴ درصد) ضرر می‌دهد. در آزمون ۱۰۰ روزهٔ واقعی روی ۱۸ بازار، هیچ‌یک از حالت‌های پیکربندی سودده نبود و همه به سقف افت سرمایهٔ خودشان خوردند (بند ۱۱-۲).

---

## ۱) خلاصهٔ یافته‌ها

1. **ورود، بعد از جهش قیمت است.** ربات زمانی می‌خرد که قیمت در چند اسکن کوتاه جهش کرده باشد (`min_observed_move_pct = 1.5%` روی ۴ اسکن ≈ ۴۰ ثانیه در پیکربندی فایل، یا `0.8%` روی ۶ اسکن در پروفایل «متعادل» که پیش‌فرض واقعی اجرا است).
2. **خروج فقط با حد ضرر و تریلینگ است.** حد سود در پیکربندی اصلی صفر (غیرفعال) است. تریلینگ در پروفایل اجراشده فقط پس از **۳٪ سود** فعال می‌شود و **۲٪** عقب‌تر از سقف می‌ایستد؛ یعنی کم‌ترین سود ممکن یک معاملهٔ برنده ≈ **+۱٪ ناخالص** است، در حالی که زیان هر معاملهٔ بازنده ≈ **−۳٪** (به‌علاوهٔ هزینه).
3. **هزینه‌ها بزرگ‌تر از هدف‌اند.** کارمزد پیش‌فرض ۰٫۲۵٪ در هر طرف (۰٫۵٪ رفت‌وبرگشت) و مجوز اسپرد تا ۰٫۹٪ (یعنی تا ۰٫۹٪ هزینهٔ ناشی از اسپرد) ⇒ رفت‌وبرگشت واقع‌بینانه **۰٫۷ تا ۱٫۴ درصد** در حالی که کم‌ترین سود تریلینگ در پیکربندی فایل **۰٫۳٪** است: «برنده»های کوچک هم خالص ضرر می‌دهند.
4. **مونت‌کارلوی هندسهٔ خروج (۱۲۰ هزار مسیر در هر سناریو):** وقتی پس از خرید هیچ روند ادامه‌داری وجود نداشته باشد، میانگین سود خالص هر معامله **−۰٫۸۹٪** است (دقیقاً برابر هزینهٔ رفت‌وبرگشت) و برای سربه‌سر شدن، بازار باید پس از سیگنال **۰٫۸۵ تا ۰٫۹ درصد دیگر** در ۱۰ دقیقهٔ بعد در همان جهت حرکت کند. این نتیجه به نوسان بازار حساس نیست (برای نوسان روزانهٔ ۲٪ تا ۱۰٪ یکسان است).
5. **در بک‌تست ۹۰ روزهٔ دادهٔ واقعی (۸ ارز، ۱۰ میلیون تومان سرمایهٔ فرضی):** پیکربندی فایل: ۴۲ معامله، نرخ برد ۴۲٫۹٪، امید ریاضی **−۰٫۶۷٪ در هر معامله**؛ پروفایل اجراشدهٔ «متعادل»: ۱۷ معامله، نرخ برد ۲۳٫۵٪، امید ریاضی **−۲٫۳۸٪ در هر معامله**. بازهٔ اطمینان بوت‌استرپ ۹۵٪: `[−۳٫۴۰ , −۱٫۱۱]` با احتمال ۱۰۰٪ منفی. در همان ۹۰ روز، خرید و نگه‌داری BTC سود ۲۸٪، ETH ۵۴٪ و AVAX ۶۵٪ می‌داد.

---

## ۲) روش بررسی

| مرحله | کار انجام‌شده |
|---|---|
| بازبینی کد | `signal_tracker.py` (ورود، خروج، تریلینگ، حد ضرر، سایزینگ، کول‌داون، halt)، `trading/nobitex_momentum_engine.py` (فیلترهای مومنتوم)، `trading/regime_detector.py`، `gui/gui_main.py` (اعمال خودکار پروفایل رژیم)، `data/bot_config.json` |
| بک‌تست دادهٔ واقعی | ۹۰ روز کندل ۱ دقیقه‌ای، ۸ ارز (BTC, ETH, SOL, XRP, DOGE, ADA, AVAX, LINK)، بازپخش کادنس اسکن، تریلینگ/حد ضرر با قیمت‌های High/Low، کارمزد و اسپرد، سایزینگ ریسک‌محور، سقف مواجهه و halt افت سرمایه |
| مونت‌کارلو | ۱۲۰٬۰۰۰ مسیر تصادفی GBM روی قواعد دقیق خروج (`tools/research/mc_exit_geometry.py`)، تحلیل نقطهٔ سربه‌سر و اثر «ادامهٔ روند» |
| کنترل‌ها | مقایسه با ورود تصادفی با همان قواعد خروج، حالت بدون هزینه (برای سنجش لبهٔ خالص سیگنال)، و خرید و نگه‌داری |

ابزارهای این مطالعه در مخزن هستند: `tools/research/fetch_market_data.py`، `tools/research/profitability_backtest.py`، `tools/research/mc_exit_geometry.py` و ورک‌فلوی `.github/workflows/research-data.yml` (نتایج در `research_results/`).

---

## ۳) چرا این استراتژی ذاتاً به لبهٔ پیش‌بینی نیاز دارد؟ (هستهٔ ریاضی)

نتیجهٔ کلیدی شبیه‌سازی: میانگین سود **ناخالص** استراتژی، مستقل از نوسان بازار، تقریباً صفر است. دلیل نظری روشن است: هر قاعدهٔ خروج (حد ضرر، تریلینگ، حد سود) یک «زمان توقف» روی یک فرایند بی‌روند است و طبق قضیهٔ توقف اختیاری، امید ریاضی قیمت خروج با قیمت ورود برابر می‌ماند؛ پس سود ناخالص ≈ ۰ و سود خالص ≈ **منهای هزینهٔ رفت‌وبرگشت**.

| نوسان روزانه | «ادامهٔ روند» لازم پس از سیگنال | میانگین سود خالص |
|---|---|---|
| ۲٪ | ۰٫۹۰٪ در ۱۰ دقیقه | ۰٪ |
| ۴٪ | ۰٫۹۰٪ در ۱۰ دقیقه | ۰٪ |
| ۶٪ | ۰٫۸۹٪ در ۱۰ دقیقه | ۰٪ |
| ۱۰٪ | ۰٫۸۵٪ در ۱۰ دقیقه | ۰٪ |

پس پرسش اصلی این است: «آیا جهش ۱٫۵ درصدی، ادامه‌دار است؟» و پاسخ آزمون واقعی: **نه به اندازهٔ کافی**. سیگنالی که بتواند ۰٫۹٪ حرکت اضافی را پیش‌بینی کند در دادهٔ ۹۰ روزه یافت نشد.

نکتهٔ فنی مهم‌تر: در پروفایل «متعادل» (که با `auto_regime_strategy: true` واقعاً اجرا می‌شود) تریلینگ تا **۳٪ سود** فعال نمی‌شود. شبیه‌سازی نشان می‌دهد با نوسان ۴٪ در روز و افق یک‌ساعته، این شرط تقریباً هرگز برآورده نمی‌شود (میانهٔ نتیجهٔ هر معامله **−۰٫۹۹٪**) و معامله عملاً با حد ضرر یا در حالت باز رها می‌شود.

---

## ۴) نتایج بک‌تست روی دادهٔ واقعی (۹۰ روز، ۸ ارز)

منبع: `research_results/results_1m.json` (دادهٔ Binance Vision، کندل ۱ دقیقه‌ای، ۱۲۹٬۶۰۰ کندل برای هر ارز، سرمایهٔ اولیه ۱۰٬۰۰۰٬۰۰۰ تومان).

| حالت | معاملات | نرخ برد | برد میانگین | باخت میانگین | امید ریاضی | ضریب سود | حداکثر افت |
|---|---|---|---|---|---|---|---|
| پیکربندی فایل `bot_config.json` | ۴۲ | ۴۲٫۹٪ | +۱٫۰۲٪ | −۱٫۹۵٪ | **−۰٫۶۷٪** | ۰٫۳۸ | ۵٫۸٪ |
| پروفایل «متعادل» (اجرای واقعی) | ۱۷ | ۲۳٫۵٪ | +۱٫۸۸٪ | −۳٫۶۹٪ | **−۲٫۳۸٪** | ۰٫۱۵ | ۱۰٫۱٪ |
| بدون هیچ هزینه‌ای (لبهٔ خالص) | ۵۰ | ۳۶٪ | +۲٫۸۳٪ | −۳٪ | **−۰٫۹۰٪** | ۰٫۵۳ | ۱۱٫۱٪ |
| ورود تصادفی (کنترل) | ۱۵ | ۱۳٫۳٪ | +۲٫۸۱٪ | −۳٫۶۹٪ | **−۲٫۸۲٪** | ۰٫۱۲ | ۱۰٫۶٪ |

**نردبان هزینه** (پیکربندی فایل): کارمزد ۰٫۱٪ و اسپرد ۰٫۰۲٪ ⇒ امید ریاضی −۰٫۱۵٪ · کارمزد ۰٫۱۵٪ و اسپرد ۰٫۱۵٪ ⇒ −۰٫۳۴٪ · کارمزد ۰٫۲۵٪ و اسپرد ۰٫۳٪ ⇒ **−۰٫۶۷٪** · کارمزد ۰٫۲۵٪ و اسپرد تا ۰٫۹٪ ⇒ **−۱٫۵۲٪**. حتی در خوش‌بینانه‌ترین سناریوی هزینه (۰٫۱٪ کارمزد، دفتر سفارش بسیار نازک)، امید ریاضی منفی است.

**مقایسه با بازار:** در همان بازه، خرید و نگه‌داری سود می‌داد (BTC +۲۸٪، ETH +۵۴٪، SOL +۵۳٪، ADA +۴۵٪، AVAX +۶۵٪). یعنی ربات در یک بازار صعودی، سرمایه را از دست داده است.

**بوت‌استرپ و مونت‌کارلوی جریان معاملات (پروفایل اجراشده):** میانهٔ ضریب نهایی سرمایه پس از ۲۰۰ معامله **۰٫۴۹۸×** (نصف شدن)، احتمال زیان ۱۰۰٪، میانهٔ حداکثر افت ۵۰٪. (n=17 معامله کوچک است؛ اما چون همهٔ برآوردها منفی‌اند، عدم قطعیت جهت نتیجه را عوض نمی‌کند.)

---

## ۵) یافته‌های کد و پیکربندی که مستقیماً به سود ضربه می‌زنند

1. **بازنویسی پیکربندی کاربر توسط «رژیم خودکار».** با `auto_regime_strategy: true` (پیش‌فرض)، تابع `_apply_auto_regime_strategy` **همهٔ فیلدهای** پروفایل انتخاب‌شده را در حافظه روی پیکربندی و هر دو tracker می‌نویسد. یعنی مقادیر `bot_config.json` (ریسک ۰٫۵٪، حداکثر ۲ پوزیشن، مواجهه ۳۰٪، تریلینگ ۱٫۵/۱٫۲) در واقع اجرا **نمی‌شوند**؛ پروفایل «متعادل» با ریسک ۰٫۷۵٪، ۱۰ پوزیشن هم‌زمان، مواجههٔ ۹۰٪، تریلینگ ۳/۲ و حد سود ۵۰٪ اجرا می‌شود. (پیشرفت tریlینگ ۲٪ برای گرفتن ۳٪ سود، و مواجههٔ ۹۰٪، ریسک را چند برابر آن‌چه کاربر انتخاب کرده می‌کند.)
2. **حد سود غیرفعال + فاصلهٔ تریلینگ بزرگ‌تر از هزینه.** در پیکربندی اصلی `take_profit_percent = 0` است و فاصلهٔ تریلینگ ۱٫۲٪ در برابر هزینهٔ ۰٫۷–۱٫۴٪ ⇒ «معاملهٔ برنده» می‌تواند خالص منفی باشد. در پروفایل «متعادل» این فاصله ۲٪ است و سود لازم برای فعال‌شدن تریلینگ ۳٪ ⇒ نیاز به نرخ برد ≈ ۸۰٪ برای سربه‌سر شدن، در حالی که نرخ برد واقعی ۲۳٫۵٪ بود.
3. **حالت paper هزینهٔ اجرا را پنهان می‌کند.** در اجرای کاغذی، اسپرد، لغزش و عمق دفتر مدل نمی‌شود؛ فقط کارمزد کسر می‌گردد. چون فاصلهٔ سربه‌سر دقیقاً روی همین ۰٫۵–۱٪ می‌چرخد، نتایج paper به‌طور سیستماتیک خوش‌بینانه‌تر از اجرای واقعی است (و `execution_mode` پیش‌فرض `paper` است).
4. **فیلتر عمق دفتر بسیار سهل‌گیرانه است.** `min_ask_depth_quote` در پیکربندی فایل ۵۰۰٬۰۰۰ تومان و در پروفایل «متعادل» ۱٬۰۰۰٬۰۰۰ تومان است؛ به‌راحتی بازارهایی که با یک سفارش، قیمت در آن‌ها ۱٫۵٪ جابه‌جا می‌شود از فیلتر عبور می‌کنند — و ماهیت همان جهش‌هایی است که ربات دنبال می‌کند.
5. **فیلتر «پرمیوم محلی» پیاده‌سازی نشده است.** `max_local_premium_pct` در BotConfig و در فهرست تنظیمات وجود دارد ولی در موتور مومنتوم استفاده نمی‌شود. این دقیقاً همان اطلاعاتی است که می‌تواند در بازار ریالی «لبه» باشد (فاصلهٔ قیمت نوبیتکس از قیمت جهانی/دلار) و بی‌استفاده مانده است.
6. **«وتوی سودآوری» با نمونهٔ ۸ معامله‌ای.** در `_apply_auto_regime_strategy`، اگر یک استراتژی ۸ معامله و امید ریاضی منفی داشته باشد، ربات می‌تواند به بهترین پروفایل مثبت سوئیچ کند — با ۸ معامله این تصمیم تقریباً تصادفی است و در بدترین حالت پروفایل «تهاجمی» (ریسک ۲٪، حد ضرر ۴٪، مواجههٔ ۹۵٪) را فعال می‌کند.
7. **نبود توقف زمانی.** ستون `max_hold_minutes` در دیتابیس هست اما هیچ منطق خروج زمانی وجود ندارد؛ معامله‌ها می‌توانند روزها باز بمانند و سرمایه را قفل کنند.
8. **توقف افت سرمایه، معاملات را در بدترین زمان قطع می‌کند.** در بک‌تست، `halted_drawdown` بارها جلوی ورود را گرفت (۷۰٬۲۳۳ وقفهٔ ثبت‌شده در پروفایل متعادل). پس از halt، ربات تا ریست دستی معامله نمی‌کند.
9. **هیچ اعتبارسنجی تاریخی در مخزن نبود.** پیش از این مطالعه، هیچ بک‌تست/forward-test قابل بازتولید در مخزن وجود نداشت که ادعای سود را پشتیبانی کند. یک تست حسابداری (`test_cumulative_partial_sell_uses_delta_quantity`) هم شکست می‌خورد؛ یعنی گزارش سود در فروش جزئی هم قابل اعتماد نیست.

---

## ۶) نقاط قوت (که در نمره لحاظ شده‌اند)

- ساختار مهندسی تمیز: دفتر کل SQLite، idempotency، محدودساز نرخ، بازیابی وضعیت، watchdog، مدیریت خاموشی، لاگ ساخت‌یافته.
- قرار دادن حد ضرر روی صرافی (`stop_market`) در حالت واقعی — یعنی در بدترین حالت، زیان هر معامله سقف دارد.
- سقف‌های ریسک: حداکثر پوزیشن، سقف نوشنال، سقف مواجهه، کول‌داون پس از باخت، فیلترهای نقدشوندگی/اسپرد/عمق، محافظ ریزش BTC، halt افت سرمایه.
- ۲۷۰ تست موفق از ۲۷۱ تست.

این‌ها ریسک را مهار می‌کنند اما **لبهٔ سود نمی‌سازند**.

---

## ۷) جدول نمره‌دهی (مرحلهٔ اول — پیش از پیاده‌سازی اصلاحات)

| معیار | وزن | امتیاز | توضیح |
|---|---|---|---|
| شواهد سوددهی و امید ریاضی | ۴۰ | ۴ | در همهٔ سناریوهای هزینه منفی؛ حتی حالت بدون هزینه در نمونهٔ ۹۰ روزه منفی |
| ساختار هزینه ↔ بازده و هندسهٔ خروج | ۲۰ | ۴ | فاصلهٔ تریلینگ کمتر از هزینه، حد سود غیرفعال، هدف سود کوچک‌تر از هزینه |
| کنترل ریسک (سقف‌ها، توقف، کول‌داون) | ۱۵ | ۱۰ | سقف‌ها و حد ضرر روی صرافی خوب؛ اما مواجههٔ ۹۰٪ و ریسک ۰٫۷۵٪ در پروفایل رژیم خودکار، تحمیل‌شده و بیش از انتخاب کاربر است |
| اعتبارسنجی، شفافیت و بازتولیدپذیری | ۱۵ | ۳ | تا امروز بک‌تستی در مخزن نبود؛ تبلیغ «سودآوری هوشمند» بدون شاهد؛ یک تست حسابداری شکست‌خورده |
| کیفیت مهندسی و پایداری | ۱۰ | ۷ | کد تمیز، تست‌ها، بازیابی وضعیت، ریسک‌محور؛ اما دکمه‌های پیکربندی مرده (`max_local_premium_pct`, `max_hold_minutes`) |
| **جمع** | **۱۰۰** | **۲۸** | ← نمرهٔ مرحلهٔ اول؛ نمرهٔ به‌روز در بند ۱۱-۶ (۳۹) |

**مقیاس تفسیر:**

| بازه | معنا |
|---|---|
| ۱–۲۰ | زیان‌دهٔ قطعی |
| ۲۱–۴۰ | **بدون لبهٔ اثبات‌شده؛ انتظار زیان — وضعیت فعلی (۲۸)** |
| ۴۱–۶۰ | مرزی؛ فقط در شرایط خاص و با هزینهٔ پایین |
| ۶۱–۸۰ | قابل استفاده با سرمایهٔ آزمایشی و پایش مستمر |
| ۸۱–۱۰۰ | اثبات‌شده در آزمون‌های متعدد با سود پس از هزینه |

نمرهٔ ۲۸ به‌معنای «این کد بد است» نیست؛ معنایش این است که **در وضعیت کنونی، اجرای آن با پول واقعی انتظار زیان دارد**.

---

## ۸) برای سودآور شدن چه باید تغییر کند (به ترتیب اهمیت)

1. **هزینه را زیر لبه بیاورید.** تا وقتی رفت‌وبرگشت ۰٫۷–۱٫۴٪ است، ورود باید فقط در سیگنال‌هایی باشد که انتظار حرکت ادامه‌دار > ۲–۳ برابر هزینه دارند. راه‌های عملی: سفارش limit (maker)، پرهیز از بازارهای با اسپرد > ۰٫۲٪، تخفیف کارمزد، و برداشتن مجوز `max_spread_pct = 0.9` (حداکثر ۰٫۲–۰٫۳ مناسب است).
2. **هندسهٔ خروج را اصلاح کنید.** با آستانهٔ ورود ۰٫۸–۱٫۵٪، استفاده از حد ضرر ۳٪ بی‌معناست: یا حد ضرر را به ≤۱٫۵٪ برسانید و فاصلهٔ تریلینگ را به ≤۰٫۵٪ کاهش دهید، یا حد سود ثابت ≈ ۲–۳ برابر هزینه بگذارید و پوزیشن را بی‌دلیل باز نگه ندارید.
3. **اولویت پیکربندی را درست کنید.** `auto_regime_strategy` نباید همهٔ فیلدها را بازنویسی کند؛ فقط `regime_controlled_keys` (یا صریحاً سقف تعداد پوزیشن) باید تغییر کند تا سقف ریسک کاربر محترم بماند.
4. **پیش از اجرای واقعی، آزمون walk-forward اجباری شود.** معیار پذیرش: امید ریاضی مثبت پس از هزینه روی حداقل ۶ ماه دادهٔ خارج از نمونه و ≥ ۲۰۰ معامله، به‌همراه فاصلهٔ اطمینان مثبت. (ابزارش اکنون در `tools/research/` هست.)
5. **حالت paper را واقع‌گرا کنید** — اسپرد و لغزش را در محاسبهٔ paper لحاظ کنید تا نتایج کاغذی گمراه‌کننده نباشد.
6. **کلید خاموشی مبتنی بر عملکرد اضافه کنید:** اگر امید ریاضی متحرک (مثلاً ۳۰ معاملهٔ آخر) منفی شد، ربات خودکار متوقف شود (نه فقط با افت سرمایه).
7. **فیلتر پرمیوم ریالی را پیاده کنید** یا حذفش کنید: تفاوت قیمت نوبیتکس با قیمت جهانی، محتمل‌ترین منبع لبهٔ واقعی در بازار IRT است.
8. **تست حسابداری فروش جزئی را درست کنید** تا سود/زیان گزارش‌شده قابل اعتماد باشد.

---

## ۹) محدودیت‌های این تحلیل

- دادهٔ تاریخی بازار ریالی نوبیتکس از محیط اجرا قابل دسترسی نبود؛ دادهٔ USDT (بایننس/بیت‌فینکس) به‌عنوان جانشین استفاده شد. ساختار سیگنال و کادنس اسکن قابل بازسازی است، اما اسپرد و عمق واقعی بازار IRT باید با اندازه‌گیری میدانی تعیین شود.
- دفتر سفارش تاریخی در دسترس نیست؛ اثر اسپرد به‌صورت پارامتری (۰٫۰۲٪ تا ۰٫۹٪) آزموده شده است.
- مطالعهٔ کندل ۱۰ ثانیه‌ای در اجرای آخر به دلیل نبود سیگنال کافی در BTC/ETH بی‌نتیجه ماند (این جفت‌ارزها در ۴۰ ثانیه تقریباً هرگز ۱٫۵٪ حرکت نمی‌کنند) — که خودش نشان می‌دهد معاملات ربات در بازارهای کم‌عمق اتفاق می‌افتد.
- تعداد معاملات در پروفایل‌ها کم (۱۷ تا ۵۰) است؛ به همین دلیل بازه‌های اطمینان و مونت‌کارلو هم گزارش شده‌اند. جهت نتیجه در همهٔ آزمون‌ها منفی است، اما بزرگی زیان قطعی نیست.
- دورهٔ آزمون (۹۰ روز گذشته) یک بازار صعودی بوده؛ در بازار نزولی نتیجهٔ این نوع استراتژی‌ها معمولاً بدتر می‌شود.

---

## ۱۰) بازتولید نتایج

```bash
python tools/research/fetch_market_data.py --out research_data --days 120 --tick-days 7 \
    --symbols BTC,ETH,SOL,XRP,DOGE,ADA,AVAX,LINK,DOT,LTC,BCH,TRX,ATOM,NEAR,FIL,UNI,AAVE,ALGO \
    --tick-symbols BTC,ETH,SOL,XRP
python tools/research/profitability_backtest.py --data-dir research_data --bars 1m --json-out out_1m.json
python tools/research/profitability_backtest.py --data-dir research_data --bars 10s --sweep --json-out out_10s.json
python tools/research/profitability_backtest.py --data-dir research_data --bars 1m --sweep \
    --sweep-cells full --symbols BTC,ETH,SOL,XRP,DOGE,ADA --json-out out_1m_sweep.json
python tools/research/mc_exit_geometry.py --paths 120000 --long --json-out mc.json
python tools/nobitex_preflight.py            # پیش‌پرواز روی دادهٔ زندهٔ نوبیتکس
```

یا به‌صورت خودکار با ورک‌فلوی `.github/workflows/research-data.yml` (اجرا روی push در `tools/research/**` و انتشار نتایج جمع‌بندی‌شده در `research_results/`).

---

## ۱۱) به‌روزرسانی: پیاده‌سازی اصلاحات و اجرای مجدد مطالعه

بخش‌های ۱ تا ۱۰ تحلیل اولیه‌اند. پس از آن‌ها، فهرست اصلاحات بند ۸ به‌صورت کد پیاده‌سازی و مطالعه با دادهٔ بیشتر و مدل خروج دقیق‌تر تکرار شد.

### ۱۱-۱) آنچه در کد تغییر کرد

| # | اصلاح | فایل | وضعیت |
|---|---|---|---|
| ۱ | گارد هزینه: رد ورود وقتی حرکت مشاهده‌شده < `min_edge_multiple ×` هزینهٔ رفت‌وبرگشت، یا وقتی فاصلهٔ تریلینگ از هزینه کمتر است (ثبت با برچسب `cost_guard`) | `signal_tracker.py` | انجام شد |
| ۲ | هزینهٔ صادقانه در حالت کاغذی: خرید به‌اندازهٔ نیم‌اسپرد بالاتر، و هر خروج (حد سود/تریلینگ/حد ضرر/توقف زمانی) به همان اندازه پایین‌تر | `signal_tracker.py` | انجام شد |
| ۳ | توقف زمانی `max_hold_minutes` (با احترام به ستون per-trade همان نام در جدول معاملات) | `signal_tracker.py` | انجام شد |
| ۴ | کلید خاموشی امید ریاضی: اگر میانگین `pnl_pct_net` آخرین N معامله منفی شود، ورودها قطع و `halt_reason` ثبت می‌شود | `signal_tracker.py` | انجام شد |
| ۵ | «رژیم خودکار» دیگر پروفایل ریسک کاربر را بازنویسی نمی‌کند؛ فقط `regime_controlled_keys` اعمال می‌شود مگر `regime_auto_apply_all` روشن باشد | `gui/gui_main.py` | انجام شد (بند ۵-۳) |
| ۶ | حساب‌داری سفارش پاره‌ای: قیمت گزارش‌شدهٔ نوبیتکس میانگین تجمعی است، پس دلتا با تفاضل قیمت‌گذاری می‌شود (`price_is_incremental` برای گزارش قیمت لحظه‌ای) | `trading/trade_accounting.py` | انجام شد (بند ۵-۹ و ۸-۸) |
| ۷ | هفت کلید جدید در `BotConfig`، همگام‌سازی در `apply_to_tracker`، و مایگریشن نسخهٔ ۱۱ (`PROFITABILITY_GUARD_DEFAULTS`) | `trading/bot_config.py` | انجام شد |
| ۸ | پروفایل پیکربندی آزمون + ابزار پیش‌پرواز + راهنمای اجرا | `data/bot_config.json`، `tools/nobitex_preflight.py`، `NOBITEX_TEST_READINESS.md` | انجام شد |

آزمون‌ها: `pytest -q tests` ⇒ **۲۷۳ تست موفق** (پیش از اصلاحات: ۱ تست شکست‌خورده).

هنوز انجام‌نشده از بند ۸: پیاده‌سازی فیلتر پرمیوم ریالی (`max_local_premium_pct`) و تبدیل سفارش‌ها به limit/maker. این دو تنها مواردی‌اند که می‌توانند **لبه** ایجاد کنند، نه فقط هزینه را مهار کنند.

### ۱۱-۲) مطالعهٔ تکرارشده (دادهٔ بزرگ‌تر، مدل خروج دقیق‌تر)

داده: ۱۸ ارز، ۱۰۰٫۷۶ روز، کندل ۱ دقیقه‌ای از Binance Vision (`research_results/results_1m.json`) + ۷ روز کندل ۱۰ ثانیه‌ای برای BTC/ETH/SOL/XRP (`results_10s.json`). هزینه‌ها: ۰٫۲۵٪ کارمزد در هر طرف + ۰٫۱۵٪ نیم‌اسپرد + ۰٫۰۵٪ لغزش ⇒ ≈ ۰٫۹٪ رفت‌وبرگشت. مدل خروج اکنون مانند حلقهٔ زنده است: استاپِ هر کندل، سطحی است که اسکن **قبلی** گذاشته، عبور قیمت از استاپ با قیمت بازشدن کندل پر می‌شود (gap)، و تریلینگ فقط وقتی سود > ۰ و ≥ سطح فعال‌سازی باشد جابه‌جا می‌شود.

| حالت | معاملات | نرخ برد | امید ریاضی | بازدهی | حداکثر افت | ضریب سود | halt |
|---|---|---|---|---|---|---|---|
| پیکربندی فایل (پیش از اصلاح) | ۱۰۲ | ۴۶٫۱٪ | **−۰٫۸۲٪** | −۱۲٫۵۰٪ | ۱۲٫۰۱٪ | ۰٫۴۳ | بله |
| پروفایل متعادل (اجرای واقعی پیش از اصلاح) | ۴۱ | ۴۸٫۸٪ | **−۰٫۵۵٪** | −۴٫۶۵٪ | ۱۰٫۰۳٪ | ۰٫۸۵ | بله |
| پروفایل محافظه‌کار | ۹۲ | ۴۷٫۸٪ | −۰٫۷۰٪ | −۱۲٫۸۵٪ | ۱۲٫۰۳٪ | ۰٫۴۵ | بله |
| بدون هزینه — پیکربندی فایل | ۱۷۵ | ۵۸٫۹٪ | −۰٫۲۶٪ | −۷٫۵۳٪ | ۱۲٫۰۲٪ | ۰٫۷۸ | بله |
| بدون هزینه — پروفایل متعادل | ۱۰۰ | ۵۰٫۰٪ | **+۰٫۲۷٪** | −۰٫۳۴٪ | ۱۰٫۰۸٪ | ۰٫۹۹ | بله |
| بدون تریلینگ — پیکربندی فایل | ۱۷ | ۱۱٫۸٪ | +۳٫۷۳٪ | +۸٫۹۱٪ | ۱۲٫۰۳٪ | ۲٫۰۸ | بله |
| بدون تریلینگ — پروفایل متعادل | ۱۰ | ۰٫۰٪ | −۷٫۷۲٪ | −۱۵٫۹۹٪ | ۱۰٫۱۵٪ | ۰٫۰۰ | بله |
| ورود تصادفی (کنترل) | ۶۱ | ۵۴٫۱٪ | −۰٫۴۰٪ | −۳٫۹۱٪ | ۶٫۹۶٪ | ۰٫۶۳ | خیر |

سه نکتهٔ کلیدی:

1. **هر حالت واقع‌گرایانه به سقف افت سرمایهٔ خودش می‌خورد و متوقف می‌شود** (۱۰٪ یا ۱۲٪) — یعنی زیان، یک رخداد دم‌دستی نیست، مسیر معمول است.
2. **لبهٔ ناخالص وجود دارد اما یک‌سوم هزینه است.** در هندسهٔ پروفایل متعادل، امید ریاضی *بدون هزینه* +۰٫۲۷٪ در هر معامله است (n=۱۰۰)؛ هزینهٔ رفت‌وبرگشت ≈ ۰٫۹٪ ⇒ خالص −۰٫۵۵٪. برای سوددهی، یا باید لبه سه برابر شود یا هزینه به زیر ۰٫۲۷٪ برسد (maker + بازار بسیار نقد).
3. **دو حالت «بدون تریلینگ» خلاف یکدیگرند** (+۳٫۷۳٪ با ۱۷ معامله در برابر −۷٫۷۲٪ با ۱۰ معامله) ⇒ این اعداد نویزِ نمونهٔ کوچک‌اند، نه لبه.

### ۱۱-۳) آزمون ادامهٔ روند (forward study) — هستهٔ استدلال

۱٬۱۶۸ رویداد «جهش ≥ ۱٫۵٪ در ۴ دقیقه» روی ۱۸ ارز. بازدهی متوسط **پس از** سیگنال:

| افق | میانگین | میانه | درصد مثبت | میانگین بیشترین سود (MFE) | میانگین بیشترین زیان (MAE) | احتمال رسیدن به فعال‌سازی تریلینگ پیش از حد ضرر |
|---|---|---|---|---|---|---|
| ۱ دقیقه | −۰٫۰۴۴٪ | −۰٫۰۵۲٪ | ۴۱٫۴٪ | +۰٫۳۱٪ | −۰٫۳۴٪ | ۲٫۷٪ |
| ۵ دقیقه | −۰٫۱۲۷٪ | −۰٫۱۷۰٪ | ۳۷٫۳٪ | +۰٫۵۵٪ | −۰٫۷۰٪ | ۵٫۹٪ |
| ۱۵ دقیقه | −۰٫۱۲۳٪ | −۰٫۱۶۵٪ | ۴۲٫۴٪ | +۰٫۸۸٪ | −۱٫۰۰٪ | ۱۸٫۴٪ |
| ۳۰ دقیقه | −۰٫۰۶۰٪ | −۰٫۰۹۵٪ | ۴۷٫۵٪ | +۱٫۲۰٪ | −۱٫۳۳٪ | ۳۰٫۰٪ |
| ۶۰ دقیقه | +۰٫۰۴۲٪ | +۰٫۱۱۸٪ | ۵۲٫۲٪ | +۱٫۵۹٪ | −۱٫۶۹٪ | ۴۲٫۶٪ |

یعنی پس از جهش، قیمت به‌طور متوسط **برمی‌گردد** (۵ تا ۳۰ دقیقه اول منفی) و تنها پس از یک ساعت به صفر می‌رسد. بزرگ‌ترین عدد این جدول (+۰٫۰۴۲٪) حدود **بیست‌ویک‌برابر کوچک‌تر** از هزینهٔ رفت‌وبرگشت (۰٫۹٪) است. این همان «لبهٔ ادامهٔ روند» است که بند ۳ می‌گوید استراتژی به آن نیاز دارد — و در داده وجود ندارد.

### ۱۱-۴) جست‌وجوی پارامتری و بررسی خارج‌ازنمونه

شبکهٔ ۱۶ سلولی با جفت‌های واقعی (فعال‌سازی، فاصله) تریلینگ روی ۶ ارز نقدشونده: **۰ سلول از ۱۶** با ≥ ۲۰ معامله امید ریاضی مثبت داشت (بهترین سلول: آستانه ۰٫۸٪، ۶ اسکن، تریلینگ ۱٫۵/۰٫۶، حد ضرر ۳٪ ⇒ −۰٫۰۲٪ در هر معامله با ۴۹ معامله و افت ۱۲٪).

> نکتهٔ روش‌شناختی: در نسخهٔ پیشین ابزار، شبکهٔ جست‌وجو «فعال‌سازی تریلینگ» را برابر «فاصلهٔ تریلینگ» تنظیم می‌کرد؛ در آن حالت سطحِ مسلح‌شده روی قیمت ورود قفل می‌شود و تقریباً همهٔ خروج‌ها روی ≈ −هزینه می‌نشینند (و حالت «تریلینگ خاموش» هم عملاً خرید و نگه‌داری تا پایان داده بود). آن نسخه ۶ سلول «مثبت» نشان می‌داد که بازتاب هندسهٔ واقعی نبود. اکنون سلول‌ها فقط جفت‌های معتبر (فعال‌سازی > فاصله) را می‌آزمایند و پنج سلول برتر روی **نیمهٔ اول و نیمهٔ دوم** بازه نیز اجرا می‌شوند (`sweep_out_of_sample` در JSON) تا سلولی که فقط در همان نیمه‌ای که انتخاب شده سود می‌دهد، به‌عنوان لبه گزارش نشود.

### ۱۱-۵) کادنس زندهٔ ۱۰ ثانیه‌ای

در ۷ روز کندل ۱۰ ثانیه‌ای برای BTC/ETH/SOL/XRP: پیکربندی فایل (۱٫۵٪ در ۴۰ ثانیه) **صفر سیگنال** و پروفایل متعادل (۰٫۸٪ در ۶۰ ثانیه) **۶ معامله** با +۲٫۲۴٪ امید ریاضی و +۳٫۲٪ بازدهی. همین بازه خرید و نگه‌داری ۳٫۲٪ تا ۹٫۱٪ سود می‌داد و کنترل تصادفی هم +۱٫۲۴٪ داشت. با n=۶ هیچ نتیجه‌ای قابل استنباط نیست — اما یک واقعیت عملیاتی را نشان می‌دهد: **در جفت‌ارزهای نقدشونده، آستانه‌های فعلی تقریباً هرگز سیگنال نمی‌دهند**، پس معاملات واقعی ربات در بازارهای کم‌عمق‌تر (با اسپرد بزرگ‌تر) رخ می‌دهد؛ دقیقاً همان‌جا که گارد هزینه باید جلوی آن‌ها را بگیرد.

### ۱۱-۶) نمرهٔ به‌روزشده

| معیار | وزن | نمرهٔ پیشین | نمرهٔ اکنون | چرا |
|---|---|---|---|---|
| شواهد سوددهی و امید ریاضی | ۴۰ | ۴ | **۵** | همچنان هیچ حالت واقع‌گرایانه‌ای سودده نیست؛ تنها نکتهٔ جدید، لبهٔ ناخالص +۰٫۲۷٪ (یک‌سوم هزینه) در هندسهٔ متعادل است |
| ساختار هزینه ↔ بازده و هندسهٔ خروج | ۲۰ | ۴ | **۶** | پیکربندی آزمون اکنون سازگار است (فاصلهٔ تریلینگ ۱٪ > هزینهٔ ۰٫۸٪؛ آستانهٔ ورود ۱٫۸٪ ≥ ۲× هزینه) اما لبهٔ اندازه‌گیری‌شده هنوز از هزینه کمتر است |
| کنترل ریسک | ۱۵ | ۱۰ | **۱۲** | رژیم خودکار دیگر ریسک کاربر را بازنویسی نمی‌کند؛ توقف زمانی و کلید خاموشی امید ریاضی اضافه شد |
| اعتبارسنجی، شفافیت و بازتولیدپذیری | ۱۵ | ۳ | **۸** | ورک‌فلوی بازتولید، شبکهٔ ۹۶ سلولی، بررسی نیمه‌به‌نیمهٔ خارج‌ازنمونه، نتایج منتشرشده در `research_results/`، ابزار پیش‌پرواز |
| کیفیت مهندسی و پایداری | ۱۰ | ۷ | **۸** | باگ حساب‌داری فروش جزئی رفع شد؛ کلیدهای مرده (`max_hold_minutes`) زنده شدند؛ ۲۷۳ تست موفق |
| **جمع** | **۱۰۰** | **۲۸** | **۳۹** | |

**تفسیر:** جهش ۲۸ → ۳۹ **به‌خاطر کشف سود جدید نیست**؛ هیچ شاهد سوددهی تازه‌ای به دست نیامد. این ۱۱ امتیاز از آنجا می‌آید که سیستم اکنون هزینه را صادقانه می‌سنجد، ریسک کاربر را بازنویسی نمی‌کند، در صورت منفی شدن امید ریاضی خودش خاموش می‌شود، و ادعاهایش با ابزار بازتولیدپذیر و دادهٔ منتشرشده پشتیبانی می‌شود. نمرهٔ ۳۹ همچنان در بازهٔ «بدون لبهٔ اثبات‌شده؛ انتظار زیان» قرار دارد.

شرط خروج از این بازه همان است که در بند ۸-۱ و ۸-۲ آمده و هیچ‌کدام هنوز پیاده نشده‌اند: **هزینهٔ زیر ۰٫۲۷٪ در هر رفت‌وبرگشت (سفارش maker و بازار بسیار نقد)** یا **سه‌برابر کردن لبهٔ سیگنال** (که امروز −۰٫۰۴٪ تا −۰٫۱۳٪ در ۵ تا ۳۰ دقیقهٔ پس از جهش است).
