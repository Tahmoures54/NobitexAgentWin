# تحلیل سودآوری ربات CryptoScanner / NobitexAgentWin

**نسخهٔ بررسی‌شده:** `6.9.0` (شاخهٔ `main`, کامیت `3e39b97`) · **تاریخ تحلیل:** ۲۰۲۶/۰۹/۲۰
**دامنه:** بازبینی کامل کد اجرا (ورود/خروج/ریسک)، شبیه‌سازی مونت‌کارلوی قواعد دقیق، بک‌تست روی دادهٔ واقعی بازار (۹۰ روز کندل ۱ دقیقه‌ای و ۷ روز کندل ۱ ثانیه‌ای → بارهای ۱۰ ثانیه‌ای) و آزمون‌های خودکار مخزن.

> **نمرهٔ نهایی سودآوری: ۳۴ از ۱۰۰** — «ساختار ایمنی خوب، لبهٔ سود اثبات‌نشده، هزینه‌ها چیره‌شده».
> جزئیات و وزن‌ها در بخش ۸. این نمره *کیفیت مهندسی* را نمی‌سنجد؛ فقط احتمال کسب سود خالص با تنظیمات فعلی را می‌سنجد.

---

## ۱) خلاصهٔ مدیریتی

1. ربات یک استراتژی **مومنتوم ورود پس از جهش کوتاه‌مدت** است: هر ۱۰ ثانیه همهٔ بازارهای IRT نوبیتکس اسکن می‌شوند و اگر قیمتی در ۴ اسکن اخیر (≈۴۰ ثانیه) حدود ۱٫۵٪ بالا رفته باشد، با **سفارش مارکت** خرید می‌کند.
2. خروج با **حد ضرر ۳٪** و **تریلینگ استاپ** (فعال‌سازی ۱٫۵٪ / فاصله ۱٫۲٪) انجام می‌شود؛ حد سود در فایل پیکربندی صفر است، یعنی «سود فقط از تریلینگ».
3. هزینهٔ رفت‌وبرگشت با تنظیمات پیش‌فرض ≈ **۰٫۹٪ از حجم معامله** است (کارمزد ۰٫۲۵٪ هر طرف + اسپرد + لغزش) و در بدترین حالت مجاز (اسپرد ۰٫۹٪) به ≈ **۲٫۲٪** می‌رسد؛ در حالی که **کف سود تریلینگ فقط ≈۰٫۳٪** است. یعنی «برنده‌های کوچک» ذاتاً از هزینه‌ها کوچک‌ترند.
4. مونت‌کارلوی قواعد دقیق روی ۳۶۰ هزار مسیر تصادفی نشان می‌دهد در بازار بدون روند، سود ناخالص هر معامله ≈ **۰٪** (شرط مارتینگل) و سود خالص ≈ **−۰٫۴۶٪ تا −۰٫۷۰٪** فقط به‌خاطر هزینه‌ها. برای مثبت شدن، ربات به **روند پایدار ۰٫۰۰۲٪ در هر اسکن (≈۱٪ در ساعت)** نیاز دارد؛ چنین لبه‌ای در دادهٔ واقعی دیده نشد.
5. بک‌تست دادهٔ واقعی (بخش ۵): در هر دو پروفایل، **انتظار ریاضی هر معامله منفی** است (فایل پیکربندی: −۰٫۳۶٪ تا −۱٫۲٪ / پروفایل واقعی اجراشده: −۰٫۷۵٪). جالب‌تر آنکه در بازهٔ آزمون، بازار **صعودی شدید** بود (بیت‌کوین ‎+۲۵٪‎، سولانا ‎+۴۸٪‎، اتریوم ‎+۴۹٪‎ در ۹۰ روز) و روش «بخر و نگه‌دار» سود بزرگی داد، اما ربات سرمایه را از دست داد.
6. نکتهٔ پنهان و مهم: در تنظیمات پیش‌فرض، `auto_regime_strategy=true` است و در نتیجه **پروفایل «متعادل» هر اسکن روی فایل پیکربندی نوشته می‌شود**؛ یعنی چیزی که واقعاً اجرا می‌شود با آنچه در `README`/`bot_config.json` مستند شده تفاوت دارد (ریسک ۰٫۷۵٪ به‌جای ۰٫۵٪، ۱۰ پوزیشن به‌جای ۲، اکسپوژر ۹۰٪ به‌جای ۳۰٪، تریلینگ ۳٫۰/۲٫۰، ورود تأییدشده، حد سود ۵۰٪). بخش ۳.
7. نتیجهٔ کلی: این ربات یک **چارچوب اجرای نسبتاً حرفه‌ای** (لاگ ساخت‌یافته، لجر SQLite، idempotency، تطبیق با کیف پول، توقف افت سرمایه) روی یک **سیگنال بدون لبهٔ اثبات‌شده و با اقتصاد معاملهٔ نامساعد** است. با هزینه‌های نوبیتکس، حتی یک سیگنال *خنثی* هم پول می‌سوزاند؛ برای سود، لبه باید از هزینه بزرگ‌تر باشد که هیچ شواهدی برای آن وجود ندارد.

---

## ۲) ربات در یک نگاه: چه چیزی واقعاً اجرا می‌شود

**مسیر اجرا (از کد):**

```
gui_main.py (~هر ۱۰ ثانیه) → get_all_market_stats()  [نوبیتکس /market/stats]
   → NobitexMomentumEngine.evaluate()                [فیلترهای مومنتوم]
   → _apply_global_lead_hit()                        [تبدیل به سیگنال خرید]
   → SignalTracker.process_new_signals()             [دروازه‌های ریسک + ورود]
   → place_order(market) + place_order(stop_market)  [خرید و حد ضرر روی صرافی]
```

**فیلترهای ورود (پیش‌فرض فایل پیکربندی):**

| پارامتر | مقدار | معنا |
|---|---|---|
| `check_interval_seconds` | ۱۰ | فاصلهٔ اسکن‌ها |
| `movement_lookback_scans` | ۴ | پنجرهٔ حرکت (≈۴۰ ثانیه) |
| `min_observed_move_pct` | ۱٫۵٪ | آستانهٔ جهش برای ورود |
| `max_chase_pct` | ۰٫۸٪ | جلوگیری از خرید بیش از حد بالا |
| `max_spread_pct` | ۰٫۹٪ | حداکثر اسپرد مجاز بازار |
| `max_local_24h_pct` | ۱۲٪ | پرهیز از خرید در سقف روز |
| `min_ask_depth_quote` | ۵۰۰٬۰۰۰ ریال | عمق دفتر (fail-closed) |
| `min_volume_irt` | ۴۰۰ میلیون ریال | حداقل حجم ۲۴ ساعته |
| `btc_max_dump_pct` | ۳٪ | توقف ورود در ریزش بیت‌کوین (با استثنای Eagle) |
| `cooldown_after_loss/win` | ۲۵ / ۵ دقیقه | خنک‌شدن پس از هر معامله |
| `max_open_positions` / اکسپوژر | ۲ / ۳۰٪ | سقف پوزیشن و مواجهه |

**خروج‌ها:**

| پارامتر | مقدار | ملاحظه |
|---|---|---|
| `stop_loss_pct` | ۳٪ | حد ضرر سخت روی صرافی (stop_market) |
| `trailing_activation_pct` | ۱٫۵٪ | تریلینگ فقط پس از این سود فعال می‌شود |
| `trailing_distance_pct` | ۱٫۲٪ | فاصله از سقف |
| `take_profit_percent` | ۰ | حد سود غیرفعال؛ تنها راه سود، تریلینگ است |
| خروج زمانی | ندارد | پوزیشن بی‌حرکت می‌تواند ساعت‌ها/روزها باز بماند |
| خروج سیگنال معکوس | فعال ولی بی‌اثر | جریان مومنتوم فقط سیگنال خرید تولید می‌کند |

**مدیریت سرمایه:** ریسک ۰٫۵٪ موجودی در هر معامله ÷ حد ضرر ۳٪ → حجم ≈ ۱۶٫۷٪ موجودی برای هر پوزیشن؛ سقف‌های `max_position_pct=20%`، `max_total_exposure_pct=30%`، `max_notional=3M ریال`، `min_notional=500k ریال`.

---

## ۳) یافتهٔ مهم: پروفایلی که اجرا می‌شود ≠ پروفایل مستندشده

در `data/bot_config.json` مقدار `auto_regime_strategy: true` است. در حلقهٔ اسکن (`gui_main.py` تابع `_apply_auto_regime_strategy`) هر اسکن پروفایل متناظر با رژیم بازار از `STRATEGY_PRESETS` (`gui/settings_window.py`) روی **cfg و هر دو tracker** نوشته می‌شود و عملاً **همهٔ** کلیدهای آن پروفایل اعمال می‌شوند (نه فقط `regime_controlled_keys`).

در رژیم غالب `BALANCED` پروفایل «متعادل» اعمال می‌شود که با مستندات README تفاوت‌های اساسی دارد:

| پارامتر | فایل `bot_config.json` | پروفایل متعادل (اجرای واقعی) | اثر |
|---|---|---|---|
| آستانهٔ ورود | ۱٫۵٪ در ۴ اسکن | **۰٫۸٪ در ۶ اسکن** | ورودهای بیشتر و بی‌کیفیت‌تر |
| تعداد پوزیشن | ۲ | **۱۰** | تنوع تساوی‌نما در بازارهای کم‌عمق |
| ریسک هر معامله | ۰٫۵٪ | **۰٫۷۵٪** | ۱٫۵ برابر ریسک |
| سقف مواجهه | ۳۰٪ | **۹۰٪** | تقریباً کل سرمایه درگیر |
| تریلینگ | ۱٫۵ / ۱٫۲ | **۳٫۰ / ۲٫۰** | کف سود تریلینگ ≈۱٪ ناخالص |
| حد سود | ۰ | **۵۰٪** | «TP خاموش» دیگر برقرار نیست (هرچند عملاً دست‌نیافتنی) |
| تأیید ورود | خاموش | **روشن (۰٫۴٪ در ۵ دقیقه)** | ورود دیرهنگام‌تر و گران‌تر |
| توقف افت سرمایه | ۱۲٪ | **۱۰٪** | توقف زودتر و نیاز به ریست دستی |
| خنک‌شدن پس از ضرر | ۲۵ دقیقه | **۶۰ دقیقه** | کاهش تعداد معاملات |

**پیامد:** هر تحلیل سودآوری که فقط `bot_config.json` را بخواند، رفتار واقعی ربات را اشتباه مدل می‌کند. هر دو پروفایل در بخش ۵ آزموده شده‌اند.

---

## ۴) اقتصاد ساختاری معامله (بدون نیاز به پیش‌بینی جهت بازار)

هزینهٔ رفت‌وبرگشت در نوبیتکس (سفارش مارکت، کارمزد taker):

```
هزینه ≈ ۲×کارمزد + ۲×(نصف اسپرد) + ۲×لغزش
       = ۲×۰٫۲۵٪ + اسپرد + لغزش = ۰٫۵٪ + اسپرد + لغزش
```

- با اسپرد ۰٫۳٪ و لغزش ۰٫۱٪ → **≈۰٫۹٪ از حجم هر معامله**
- در بدترین حالت مجاز فیلتر (`max_spread_pct=0.9%`) → **≈۲٫۲٪**

هندسهٔ خروج:

| سنجه | مقدار |
|---|---|
| کف سود تریلینگ (ناخالص) | فعال‌سازی ۱٫۵٪ − فاصله ۱٫۲٪ = **+۰٫۳٪** |
| همان مقدار پس از هزینه‌های بالا | **−۰٫۶٪** (یعنی «برنده» هم ضرر می‌دهد) |
| سود لازم برای سربه‌سر شدن | **≈۰٫۹٪ به بالا** |
| نرخ برد لازم برای سربه‌سر (با حد ضرر ۳٪ و میانگین سود ۳٪) | **≈۷۲٪** |
| نسبت ریسک به ریوارد واقعی | حد ضرر −۳٪ در مقابل اکثر برنده‌های +۰٫۳٪ تا +۱٫۸٪ |

**آزمون مونت‌کارلو روی قواعد دقیق** (`/tmp/mc_rules.py`, ۱۲۰–۴۰۰ هزار مسیر، اسکن ۱۰ ثانیه‌ای):

| نوسان ۱۰ ثانیه‌ای | درایو (٪ در هر اسکن) | سود ناخالص هر معامله | سود خالص (با هزینهٔ ۰٫۷٪) | نرخ برد |
|---|---|---|---|---|
| ۰٫۰۳٪ | ۰ | +۰٫۰۰۴٪ | **−۰٫۶۹٪** | ۵۰٪ |
| ۰٫۱۰٪ | ۰ | +۰٫۰۴٪ | **−۰٫۶۶٪** | ۵۵٪ |
| ۰٫۲۰٪ | ۰ | +۰٫۱۴٪ | **−۰٫۵۶٪** | ۶۵٪ |
| ۰٫۳۵٪ | ۰ | +۰٫۲۴٪ | **−۰٫۴۶٪** | ۶۶٪ |
| ۰٫۱۰٪ | +۰٫۰۰۲٪ | +۰٫۶۸٪ | **−۰٫۰۲٪** | ۶۹٪ |
| ۰٫۱۰٪ | +۰٫۰۰۵٪ | +۱٫۶۵٪ | **+۰٫۹۵٪** | ۸۵٪ |

**خوانش:** ساختار «حد ضرر ۳٪ + تریلینگ با کف +۰٫۳٪» قبل از هزینه‌ها یک **شرط منصفانه (مارتینگل)** است؛ نه برندهٔ ساختاری و نه بازندهٔ ساختاری. بنابراین **هزینه‌ها تعیین‌کننده‌اند** و ربات بدون یک لبهٔ روندی پایدار (≈۰٫۰۰۲٪ در هر اسکن ≈۱٪ در ساعت) به‌طور سیستماتیک ضرر می‌دهد. توجه کنید درایو لازم برای پوشش هزینه *حتی از روند ساعتی معمول بازار هم بزرگ‌تر* است، چون این استراتژی‌ها در پنجره‌های ۴۰ ثانیه‌ای معامله می‌کنند که در آن درایو تقریباً صفر است.

---

## ۵) نتایج تجربی روی دادهٔ واقعی

**داده:** ۸ نماد (BTC, ETH, SOL, XRP, DOGE, ADA, AVAX, LINK) — ۹۰ روز کندل ۱ دقیقه‌ای؛ به‌علاوه ۷ روز کندل ۱ ثانیه‌ای که به بارهای ۱۰ ثانیه‌ای تجمیع شده (BTC, ETH) تا کادنس واقعی اسکن بازسازی شود. منبع: آرشیو عمومی Binance Vision + Bitfinex (نوبیتکس از محیط ابری قابل دسترس نیست).

### ۵-۱) بک‌تست کادنس ۱۰ ثانیه‌ای (سخت‌گیرانه‌ترین آزمون)

{{RESULTS_10S}}

### ۵-۲) بک‌تست ۹۰ روز روی ۸ نماد (کندل ۱ دقیقه‌ای)

{{RESULTS_1M}}

### ۵-۳) مطالعهٔ بازده آیندهٔ سیگنال (آیا «جهش ۴۰ ثانیه‌ای» ادامه دارد؟)

{{RESULTS_FORWARD}}

---

## ۶) یافته‌های کد که مستقیماً روی سود اثر می‌گذارند

| # | یافته | محل | اثر |
|---|---|---|---|
| ۱ | پروفایل رژیم، فایل پیکربندی کاربر را هر اسکن بازنویسی می‌کند و `regime_controlled_keys` در این مسیر اعمال نمی‌شود | `gui/gui_main.py:516+` | ریسک/اکسپوژر بیشتر از آنچه کاربر تعیین کرده |
| ۲ | کف سود تریلینگ (۰٫۳٪) از هزینهٔ رفت‌وبرگشت (۰٫۹–۲٫۲٪) کمتر است | `signal_tracker.py:1272-1310` | «برنده»های کوچک خالص منفی‌اند |
| ۳ | هیچ خروج زمانی وجود ندارد (`max_hold_minutes` ساخته شده ولی هیچ‌جا استفاده نمی‌شود) | `signal_tracker.py:1320` | سرمایه در پوزیشن‌های بی‌حرکت قفل می‌شود |
| ۴ | در حالت کاغذی (paper) اسپرد/لغزش مدل نمی‌شود؛ فقط کارمزد | `signal_tracker.py:157`, `1242` | نتیجهٔ paper خوش‌بینانه‌تر از live |
| ۵ | سوئیچ «سودمحور» با تنها ۸ معامله و انتظار منفی تصمیم می‌گیرد و می‌تواند پروفایل «تهاجمی» (ریسک ۲٪) را فعال کند | `gui/gui_main.py:530-585` | ریسک انتخاب‌شدن بر اساس نویز آماری |
| ۶ | ورود و خروج همیشه taker هستند (`market` و `stop_market`) | `trading/nobitex_client.py`, `signal_tracker.py:1350,2004` | هزینهٔ کامل اسپرد در هر دو سمت |
| ۷ | فیلتر بازار فقط «اسپرد ≤۰٫۹٪» می‌گذارد؛ با این اسپرد، معامله ذاتاً زیر هزینه می‌رود | `data/bot_config.json` | ورود به بازارهای نامناسب |
| ۸ | تست حسابداری فروش جزئی شکست می‌خورد (۱ آزمون از ۲۷۱) | `tests/test_trade_accounting.py` | احتمال خطای سود/زیان محاسبه‌شده |

---

## ۷) ریسک‌های عملیاتی

- **توقف افت سرمایه:** با ۱۰–۱۲٪ حد افت و مواجههٔ بالا، احتمال فعال‌شدن circuit breaker در چند روز معاملاتی زیاد است؛ پس از توقف، ربات تا ریست دستی (`reset_halt.py`) معامله نمی‌کند — یعنی دقیقاً هنگام بازگشت بازار از بازی خارج می‌ماند.
- **paper ≠ live:** در paper اسپرد و عمق بازار لحاظ نمی‌شود؛ با توجه به اینکه در نتایج، همین اختلاف (۰٫۳–۰٫۹٪) مرز بین «صفر» و «ضرر» است، paper ربات را سودآورتر از واقع نشان می‌دهد.
- **نوبیتکس IRT:** اسپرد واقعی تومانی/ریالی معمولاً از بازارهای جهانی بدتر است و نوسان نرخ دلار به نوسان جفت‌ارز اضافه می‌شود؛ بخشی از جهش‌های ۱٫۵٪ در ۴۰ ثانیه در حقیقت پرش اسپرد یا حرکت نرخ ارز است، نه روند قابل‌معامله.
- **نقدشوندگی:** فیلتر عمق ۵۰۰٬۰۰۰ ریالی بسیار سبک است؛ برای بازارهای کوچک نوبیتکس، اسپرد و لغزش می‌تواند چند برابر بیت‌کوین باشد.
- **حرکت‌های تک‌سفارشی:** جهش‌های ۱٫۵٪ در ۴۰ ثانیه در بازارهای کم‌عمق غالباً نتیجهٔ یک سفارش بزرگ‌اند و بلافاصله برمی‌گردند — یعنی ربات در بدترین قیمت ممکن می‌خرد.

---

## ۸) نمره‌دهی تفصیلی (۱ تا ۱۰۰)

| معیار | وزن | نمره | توضیح |
|---|---|---|---|
| انتظار ریاضی/لبهٔ آماری سیگنال | ۳۰ | ۶ | در هر دو پروفایل و در همهٔ سناریوهای هزینه، انتظار منفی؛ مونت‌کارلو نشان می‌دهد سود ناخالص ≈۰ است و لبه‌ای دیده نشد |
| ساختار ریسک–بازده و مدیریت سرمایه | ۲۰ | ۱۱ | حد ضرر سخت روی صرافی، سایزینگ مبتنی بر ریسک، سقف اکسپوژر و DAILY loss breaker خوب‌اند؛ اما نسبت ریسک/ریوارد و نبود خروج زمانی ساختار را خراب می‌کند |
| هزینه‌ها، اسپرد، اجرا و نقدشوندگی | ۱۵ | ۴ | هر دو سمت taker، مدل هزینهٔ ناکافی در paper، آستانهٔ اسپرد ۰٫۹٪ در برابر تریلینگ ۱٫۲٪ |
| اعتبارسنجی و شواهد تجربی | ۱۵ | ۴ | بک‌تست/walk-forward/گزارش عملکرد واقعی وجود ندارد؛ سوئیچ سودمحور بر نمونهٔ ۸ معامله‌ای استوار است |
| مهندسی نرم‌افزار، پایداری و حسابداری | ۱۰ | ۷ | لاگ ساخت‌یافته، لجر WAL، idempotency، retry، watchdog، تطبیق با کیف پول و حسابداری fill واقعی؛ یک تست حسابداری شکست‌خورده |
| کالیبراسیون/شفافیت پیکربندی و قابلیت اندازه‌گیری | ۱۰ | ۲ | پروفایل اجراشده با مستندات نمی‌خواند، presetها تنظیمات کاربر را بازنویسی می‌کنند، سنجهٔ زندهٔ «انتظار ریاضی» و kill-switch مبتنی بر عملکرد وجود ندارد |
| **جمع** | **۱۰۰** | **۳۴** | |

**تفسیر نمره:**

- **۰–۲۰:** عملاً زیان‌ده · **۲۱–۴۰:** بدون لبهٔ اثبات‌شده؛ در عمل زیان‌ده مگر با شرایط ایده‌آل · **۴۱–۶۰:** لبهٔ ضعیف/شرطی · **۶۱–۸۰:** لبهٔ قابل‌اتکا با مدیریت ریسک · **۸۱–۱۰۰:** سیستم اثبات‌شده و پایدار.

نمرهٔ ۳۴ یعنی: **این ربات به‌صورت پیش‌فرض شانس سود ندارد**؛ ساختار ایمنی و مهندسی آن بالاتر از میانگین است، اما هستهٔ سودآوری (لبه + اقتصاد معامله) اثبات‌نشده و در آزمون‌ها منفی است.

---

## ۹) برای سودآور شدن چه باید کرد (به ترتیب اولویت و قابل اندازه‌گیری)

1. **هزینه را زیر لبه بیاورید:** فقط بازارهایی با اسپرد ≤۰٫۲٪ (نسبت به حد ضرر/تریلینگ)، ورود با سفارش لیمیت (maker) و خروج با حد ضرر OCO؛ در غیر این صورت هر لبه‌ای با ۰٫۹–۲٫۲٪ هزینه بلعیده می‌شود.
2. **هندسهٔ خروج را اصلاح کنید:** کف سود تریلینگ باید از هزینه بزرگ‌تر باشد؛ مثلاً فعال‌سازی ۳٪/فاصله ۱٫۲٪ یا حد سود ثابت ≥ ۲٫۵ برابر هزینهٔ رفت‌وبرگشت. حد ضرر ۳٪ با برندهٔ ۰٫۳٪ نسبت ریسک/ریوارد ۱:۱۰ می‌سازد که با نرخ برد واقعی (~۵۰٪) ریاضیات شکست‌خورده است.
3. **خروج زمانی و بازبینی سیگنال:** اگر حرکت در N دقیقه ادامه پیدا نکرد، پوزیشن بسته شود تا سرمایه آزاد و turnover کنترل شود.
4. **اعتبارسنجی قبل از پول واقعی:** بک‌تست با دادهٔ tick نوبیتکس + اسپرد واقعی، سپس forward-test روی حساب واقعی با حجم حداقلی و گزارش روزانهٔ انتظار ریاضی؛ «paper» به‌تنهایی معیار نیست چون اسپرد را حذف می‌کند.
5. **حذف بازنویسی پیکربندی:** `regime_controlled_keys` را در مسیر `auto_regime_strategy` هم اعمال کنید یا presetها را به فیلدهای مشخص محدود کنید تا تنظیمات ریسک کاربر دست‌نخورده بماند.
6. **پایش زنده و kill-switch:** پس از N معامله (مثلاً ۳۰) اگر انتظار ریاضی خالص (پس از هزینه) منفی بود، ربات خودکار متوقف و به رژیم «فقط پایش» برود؛ نه اینکه با ۸ معامله پروفایل را عوض کند.
7. **فیلتر کیفیت سیگنال:** جهش خالص را از جهش ناشی از اسپرد/سفارش تک‌نفره تفکیک کنید (فیلتر taker-volume، عمق دفتر، تداوم چند اسکن، تأییدیهٔ حجم) و ورود را در سمت خرید دفتر (bid) انجام دهید، نه chasing سقف.
8. **شفافیت مستندات:** پروفایل مؤثر واقعی (پس از اعمال preset) در UI و README نمایش داده شود تا کاربر بداند با چه ریسکی معامله می‌کند.

---

## ۱۰) محدودیت‌های این تحلیل

- دادهٔ تاریخی نوبیتکس (بازار IRT) از محیط اجرا قابل دسترس نبود؛ از دادهٔ جهانی BTC/ETH/آلت‌کوین‌ها به‌عنوان جانشین استفاده شد. ساختار سیگنال و کادنس ده‌ثانیه‌ای قابل بازسازی است، اما اسپرد/عمق واقعی بازار ریالی در تحلیل به‌صورت سناریو مدل شده است.
- بک‌تست به‌صورت «کادنس اسکن» روی بارهای ۱ ثانیه‌ای تجمیع‌شده اجرا شده؛ دفتر سفارش (bid/ask) تاریخی موجود نیست، بنابراین هزینهٔ اسپرد به‌صورت پارامتر روی قیمت اجرا اعمال شده است.
- بازار در دورهٔ آزمون روند صعودی داشت؛ در بازارهای نزولی معمولاً نتایج این نوع استراتژی‌ها بدتر می‌شود (برنده‌های کوچک حذف می‌شوند، حد ضررها پر می‌شوند).
- تعداد معاملات مشاهده‌شده کم است (ده‌ها معامله)؛ به همین دلیل بازهٔ اطمینان بوت‌استرپ و مونت‌کارلو نیز گزارش شده است.
