# آماده‌سازی تست با دادهٔ نوبیتکس

این سند دستور اجرای تست کاغذی (paper) روی دادهٔ زندهٔ نوبیتکس را می‌دهد؛ همان چیزی که پس از تحلیل سودآوری (`PROFITABILITY_ANALYSIS.md` — نمرهٔ **۴۰ از ۱۰۰**، مرحلهٔ اول ۲۸ بود) به‌صورت کد عملیاتی شده است.

> **انتظار واقع‌بینانه:** مطالعهٔ ۱۰۰ روزه روی ۱۸ بازار واقعی نشان داد هیچ پروفایلی سودده نیست (با همین پیکربندی: امید ریاضی −۰٫۴۶٪ تا −۰٫۷۴٪ در هر معامله) و همهٔ حالت‌ها به سقف افت سرمایهٔ خودشان می‌خورند. لبهٔ ناخالص سیگنال — یعنی حتی اگر کارمزد و اسپرد **صفر** باشد — تنها **+۰٫۰۴٪ در هر معامله** است، حدود **یک‌بیستم** هزینهٔ رفت‌وبرگشت (۰٫۸٪). در شبکهٔ ۹۶ سلولیِ هندسهٔ خروج هم با توقف زمانیِ همین پروفایل **هیچ سلولی بازدهی مثبت نداشت** و بدون آن، هیچ‌کدام از ۵ سلول برتر در آزمون نیمه‌به‌نیمهٔ خارج‌ازنمونه دوام نیاورد. جزئیات: `PROFITABILITY_ANALYSIS.md` بندهای ۱۱-۲، ۱۱-۴ و ۱۱-۶.
>
> هدف این تست، **تأیید سوددهی نیست**؛ هدف این است که با هزینهٔ صادقانه، گاردهای محافظ و دادهٔ واقعی نوبیتکس اندازه‌گیری کنیم و اگر نتیجه منفی بود، ربات خودش متوقف شود — نه اینکه با آمار خوش‌بینانه گمراه شویم. آنچه این تست می‌تواند به آن پاسخ دهد و بک‌تست نمی‌تواند: **اسپرد و پرشدنِ واقعیِ سفارش در نوبیتکس چقدر است** و **آیا گارد هزینه در بازار واقعی به‌درستی ورودهای بی‌صرفه را رد می‌کند**.

---

## ۱) چه چیزی در این نسخه عوض شد

| # | تغییر | فایل | اثر |
|---|---|---|---|
| ۱ | **گارد هزینه (cost guard)** | `signal_tracker.py` | ورودی که حرکت مشاهده‌شده‌اش کمتر از `min_edge_multiple` برابر هزینهٔ رفت‌وبرگشت است رد می‌شود (و نیز وقتی فاصلهٔ تریلینگ از هزینه کمتر باشد)؛ دلیل رد با برچسب `cost_guard` در لاگ اسکیپ‌ها ثبت می‌شود. |
| ۲ | **هزینهٔ صادقانه در حالت کاغذی** | `signal_tracker.py` | هر خرید `paper_half_spread_pct` بالاتر و هر فروش همان مقدار پایین‌تر از میانه پر می‌شود؛ دیگر «برد»های کاغذی رایگان نیستند. |
| ۳ | **توقف زمانی** | `signal_tracker.py` | پوزیشنی که بیش از `max_hold_minutes` باز بماند با دلیل «Time Stop» بسته می‌شود (حد سود در این پیکربندی هرگز فعال نمی‌شد). مقدارِ پروفایل آزمون **۳۶۰ دقیقه** است؛ دلیلش در بند ۸ زیر آمده. |
| ۴ | **گارد امید ریاضی** | `signal_tracker.py` | اگر میانگین `pnl_pct_net` آخرین N معاملهٔ بسته‌شده از آستانه کمتر شود، ورودهای جدید خودکار قطع و `halt_reason` ثبت می‌شود. |
| ۵ | **محدود کردن اعمال خودکار رژیم** | `gui/gui_main.py` | اسکن هر ۲۰ ثانیه دیگر پروفایل ریسک کاربر را بازنویسی نمی‌کند؛ فقط کلیدهای `regime_controlled_keys` (پیش‌فرض: `max_open_positions`) اعمال می‌شوند مگر `regime_auto_apply_all` روشن شود. |
| ۶ | **اصلاح حساب‌داری سفارش‌های پاره‌ای** | `trading/trade_accounting.py` | قیمت گزارش‌شدهٔ هر پیام فقط برای همان مقدار «دلتا» ثبت می‌شود؛ سفارش‌های نوبیتکس که قیمت میانگین می‌فرستند با پرچم `price_is_cumulative_average` علامت می‌خورند تا دوباره‌شماری نشود. |
| ۷ | **همگام‌سازی با GUI و مایگریشن** | `trading/bot_config.py` | ۷ کلید جدید به `BotConfig`، `apply_to_tracker` و بلوک نسخهٔ ۱۱ (`PROFITABILITY_GUARD_DEFAULTS`) اضافه شد تا پیکربندی‌های قدیمی هم گاردها را بگیرند. |
| ۸ | **تغییر `max_hold_minutes` از ۱۲۰ به ۳۶۰ دقیقه** | `data/bot_config.json`، `trading/bot_config.py` | بر پایهٔ حساسیت‌سنجی سه‌سطحی (۱۲۰ / ۳۶۰ / بدون سقف) روی همان داده: بدون سقف ۶۲٪ معاملات بیش از ۲ ساعت باز می‌مانند، پس سقفِ ۱۲۰ دقیقه‌ای تریلینگ را کنار می‌زند و خودش خروجِ غالب می‌شود (۴۶ خروج از ۶۱). هیچ سطحی سودده نیست؛ ۳۶۰ دقیقه سقفی است که **اجازه می‌دهد تست، استراتژی را بسنجد نه سقف را**. جدول کامل: `PROFITABILITY_ANALYSIS.md` بند ۱۱-۶. |

آزمون‌ها: `pytest -q tests` ⇒ **۲۸۸ تست موفق** (۱۵ تست جدید: ۵ رگرسیونِ موتور بازپخشِ مطالعه، ۷ ناوردای اقتصادِ پروفایل ارسال‌شده، ۳ تقدمِ مایگریشن نسخهٔ ۱۱).

---

## ۲) پیش‌نیازها

```bash
python -m pip install -r requirements.txt      # Python 3.9+ (توسعه با 3.11)
```

- **حالت کاغذی** برای این تست کافی است؛ به کلید API نیازی نیست (دادهٔ بازار عمومی است).
- فقط برای حالت واقعی، کلید عمومی و کلید خصوصی Ed25519 نوبیتکس لازم است.
- این ماشین (سندباکس توسعه) به دامنهٔ نوبیتکس دسترسی DNS ندارد؛ اسکریپت و ربات را روی همان سیستمی اجرا کنید که قرار است معامله کند.

---

## ۳) گام ۱ — پیش‌پرواز (قبل از روشن کردن ربات)

```bash
python tools/nobitex_preflight.py            # آنلاین: اتصال + اسپرد واقعی + گارد هزینه
python tools/nobitex_preflight.py --offline  # آفلاین: فقط حساب‌وکتاب هزینه
```

خروجی به شما می‌گوید:

- هزینهٔ رفت‌وبرگشت واقعی (۲ × کارمزد + اسپرد واقعی هر جفت‌ارز) در برابر فاصلهٔ تریلینگ؛
- کم‌ترین حرکت لازم برای عبور از گارد هزینه؛
- اینکه همین حالا کدام جفت‌ارزها رد می‌شوند و چرا.

اگر پیش‌پرواز «🔴» داد، ابتدا پیکربندی را اصلاح کنید؛ روشن کردن ربات با هندسهٔ ناسازگار فقط لاگ `cost_guard` تولید می‌کند.

---

## ۴) گام ۲ — اجرای ربات در حالت کاغذی

```bash
python main.py
```

سپس در GUI:

1. حالت اجرا: **Paper** (در `data/bot_config.json` مقدار `execution_mode` برابر `paper` است).
2. دکمهٔ اسکن زنده/معاملات خودکار را روشن کنید.
3. ربات هر ۱۰ ثانیه (طبق `check_interval_seconds`) بازار را می‌خواند و هر ۲۰ اسکن وضعیت رژیم را می‌سنجد.

### تنظیمات آزمون (در `data/bot_config.json`)

| کلید | مقدار | چرا |
|---|---|---|
| `trading_fee_pct` | `0.25` | کارمزد واقعی هر طرف ⇒ ۰٫۵٪ رفت‌وبرگشت |
| `paper_half_spread_pct` | `0.15` | شبیه‌سازی اسپرد واقعی در هر طرف |
| `cost_guard_enabled` | `true` | ورود فقط اگر اقتصاد معامله بسته شود |
| `min_edge_multiple` | `2.0` | حرکت مشاهده‌شده باید ≥ ۲ برابر هزینهٔ رفت‌وبرگشت باشد |
| `min_observed_move_pct` / `pump_threshold_pct` | `1.8` | آستانهٔ سیگنال بالاتر از کفِ ۱٫۶٪ گارد هزینه |
| `trailing_activation_pct` / `trailing_distance_pct` | `3.0` / `2.0` | فاصلهٔ تریلینگ ۱٪ > هزینهٔ ۰٫۸٪ ⇒ کوچک‌ترین «برد» تریلینگ خالص مثبت است |
| `stop_loss_pct` | `3.0` | حد ضرر ثابت روی هر ورود |
| `max_hold_minutes` | `360` | توقف زمانی؛ جلوی پوسیدن پوزیشن‌های بی‌حرکت را می‌گیرد. ۳۶۰ نه ۱۲۰: سقفِ ۲ ساعته روی ۶۲٪ معاملاتِ مطالعه سوار می‌شد و تریلینگ را قبل از فعال‌شدن قطع می‌کرد (بند ۱۱-۶ تحلیل) |
| `expectancy_guard_enabled` / `..._trades` / `..._min_expectancy_pct` | `true` / `30` / `0.0` | اگر میانگین ۳۰ معاملهٔ آخر منفی شد، ورود قطع می‌شود |
| `regime_controlled_keys` + `regime_auto_apply_all=false` | `["max_open_positions"]` | اسکن رژیم دیگر ریسک کاربر را بازنویسی نمی‌کند |
| `execution_mode` | `paper` | تست بدون پول واقعی |

---

## ۵) گام ۳ — چه چیزی را رصد کنیم

| نشانگر | کجاست | معنی |
|---|---|---|
| تعداد ردهای `cost_guard` | لاگ اسکیپ‌ها / دیتابیس `data/signal_log.db` | اگر تقریباً همهٔ سیگنال‌ها رد شوند، اسپرد بازار نسبت به آستانهٔ ورود بزرگ است؛ عدد را ثبت کنید، نه اینکه گارد را خاموش کنید. |
| قیمت پر شدن کاغذی | جدول معاملات | باید به‌اندازهٔ نیم‌اسپرد بدتر از «قیمت میانه در لحظهٔ سیگنال» باشد؛ اگر برابر بود، شبیه‌سازی هزینه کار نمی‌کند. |
| دلیل خروج‌ها | ستون `exit_reason` | ترکیب «Trailing Stop / Stop Loss / Time Stop» نشان می‌دهد کدام پای هندسه کار می‌کند. در مطالعهٔ ۱۰۰ روزه با همین پروفایل، هندسهٔ فایل ۵۸ تریلینگ / ۳۴ حد ضرر / ۱۱ توقف زمانی از ۱۰۴ معامله بود و پروفایل متعادل ۱۳ / ۹ / ۲۷ از ۵۳. **اگر در تست شما `Time Stop` بیش از نیمی از خروج‌ها شد، تریلینگِ ۳٪ در بازارِ شما فعال نمی‌شود** و نتیجه را سقف زمانی می‌سازد، نه استراتژی — این را به‌عنوان یافته ثبت کنید. |
| `pnl_pct_net` | معاملات بسته‌شده | همان عددی که گارد امید ریاضی می‌خواند؛ مبنای قضاوت نهایی. |
| `halt_reason` | وضعیت ربات | اگر «expectancy guard» یا «max drawdown» پر شد، ربات خودش دست نگه داشته است. |

---

## ۶) گام ۴ — قاعدهٔ تصمیم

پس از **حداقل ۳۰ معاملهٔ بسته‌شده** (یا ۱۴ روز، هرکدام زودتر رسید):

- **ادامه بده** اگر: امید ریاضی خالص > ۰، ضریب سود (PF) > ۱٫۲ و حداکثر افت سرمایه < ۵٪.
- **متوقف کن و بازنگری کن** اگر: امید ریاضی ≤ ۰ — حتی اگر نرخ برد بالا باشد (نرخ برد بالا با باخت‌های بزرگ همان دام تحلیل قبلی است).
- نرخ برد به‌تنهایی معیار نیست؛ با هندسهٔ فعلی (باخت ۳٪ + هزینه در برابر کم‌ترین برد ۱٪ − هزینه) نرخ برد سربه‌سر بسیار بالاست و «۶۰٪ برد» هم می‌تواند زیان‌ده باشد.

برای تحلیل بعدی این‌ها را نگه دارید: `data/signal_log.db` (جدول‌های معاملات و اسکیپ‌ها)، لاگ اجرای همان بازه، و خروجی `tools/nobitex_preflight.py` در روزهای مختلف (برای ثبت اسپرد واقعی هر جفت‌ارز).

---

## ۷) عیب‌یابی سریع

| نشانه | علت محتمل | راه‌حل |
|---|---|---|
| `Nobitex network error` در پیش‌پرواز | نبود دسترسی به دامنهٔ نوبیتکس (DNS/فیلترینگ) | اجرای روی سیستم دارای دسترسی، یا تنظیم پروکسی سیستمی |
| هیچ معامله‌ای باز نمی‌شود و لاگ پر از `cost_guard` است | اسپرد واقعی بزرگ‌تر از فاصلهٔ تریلینگ | یا جفت‌ارز نقدشونده‌تر انتخاب کنید یا (آگاهانه) `min_edge_multiple` و فاصلهٔ تریلینگ را بازتنظیم کنید — این عدد را به‌عنوان یافتهٔ تست ثبت کنید |
| ورودها باز می‌شوند اما بیشتر با «Time Stop» بسته می‌شوند | بازار حرکت ادامه‌دار کافی برای فعال‌سازی تریلینگِ ۳٪ نمی‌دهد (همان یافتهٔ تحلیل: در کادنس ۱۰ ثانیه‌ای با سقف ۱۲۰ دقیقه، ۶ خروج از ۸ خودِ سقف بود و امید ریاضی از +۲٫۲۴٪ به −۰٫۹۸٪ رفت) | نتیجه را ثبت کنید؛ این یعنی لبه‌ای برای پوشش هزینه وجود ندارد. گارد را خاموش نکنید و سقف را هم بالا نبرید تا نتیجه «خوب» شود — آن کار فقط اندازه‌گیری را خراب می‌کند |
| `halt_reason = expectancy guard` | میانگین ۳۰ معاملهٔ آخر منفی بوده | ربات درست کار کرده؛ تست را با تغییر استراتژی/هندسه از سر بگیرید، نه با خاموش کردن گارد |
| خطای «Invalid Nobitex private key» | کلید اشتباه/ناقص برای حالت واقعی | در حالت کاغذی کلید لازم نیست؛ برای واقعی، کلید Ed25519 را از پنل نوبیتکس بسازید |

---

## ۸) پاک کردن حافظهٔ تست

برای شروع یک تست تمیز (حذف معاملات و اسکیپ‌های قبلی):

```bash
python reset_halt.py          # برداشتن توقف اضطراری
# سپس دیتابیس لاگ را از data/ جابه‌جا یا حذف کنید تا آمار از صفر شروع شود
```

پیش از حذف، از `data/signal_log.db` نسخهٔ پشتیبان بگیرید؛ همان دادهٔ خام تحلیل بعدی است.
